# clonarnos el repositorio
cd /c/Workspace/Github/
git clone https://github.com/juanlbravov/projectPython.git
git log 
ls -la

# creamos el repositorio
git init
git remote -v
git remote add origin git@github.com:juanlbravov/projectPython.git
git branch -all

# push
git add .
git commit -m "
git branch -M main
git push -u origin main

# pull
git pull origin main